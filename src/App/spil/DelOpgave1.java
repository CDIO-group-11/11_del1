package App.spil;

class DelOpgave1 {
public static void main(String[] args) {
    RaffleCup cup = Main.getCup();
    // Sætter de to terninger 
    int Die1 = cup.getSides()[0];
    int Die2 = cup.getSides()[1];
    // mangler check hvem spiller 
    

    if (Die1 == Die2);
    // Følgende linje er 
    // PlayerPoint = 0;

}


}
